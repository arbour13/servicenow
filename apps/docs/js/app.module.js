/* Standalone GlideFast Docs - hosts reference documentation (search, group/page/section
   navigation, expand/collapse, cross-page [[links]], light/dark theme, deep-linkable sections).
   Read-only: content is authored in markdown and reviewed through git, not edited in the browser.
   No script builders, no ServiceNow connection features - just the docs. */
angular.module('glidefastDocs', []);
