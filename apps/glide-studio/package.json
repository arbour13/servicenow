{
  "name": "glide-studio-angular",
  "private": true,
  "description": "Build tooling for Glide Studio's SCSS source (scss/app.scss -> css/app.css). No runtime dependency in the deployed widget - dev/authoring-only. Deployment packaging is not this app's job - see deploy.manifest.js and the shared tools/sn-deployment-packager/ (standalone deploy console + Node build.js CLI).",
  "scripts": {
    "build:css": "sass scss/app.scss css/app.css --no-source-map",
    "watch:css": "sass --watch scss/app.scss css/app.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.101.0"
  }
}
