/* Standalone GlideFast Standards Portal - hosts only the scripting best-practices reference
   document (search, chapter/section navigation, expand/collapse, light/dark theme, deep-linkable
   sections). No script builders, no ServiceNow connection features - just the doc. */
angular.module('standardsPortal', []);
