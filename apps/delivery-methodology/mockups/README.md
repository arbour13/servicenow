# Design explorations — not the app

Historical mockups and snapshots kept for reference; nothing here is loaded by the app.

- `_mockups.html`, `layout-mockups.html`, `style-mockups.html` — early layout/style explorations.
- `delivery-methodology-artifact.html` — a 2026-07-19 snapshot of the standalone prototype
  (the *live* prototype is `../delivery-methodology.html`, dual-maintained with the AngularJS app).

The real app is `../index.html` (+ `../js/`, `../scss/`).
